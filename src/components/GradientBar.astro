<div class="gradient-bar"></div>

<style>
	.gradient-bar {
		display: flex;
		background: linear-gradient(
			to right,
			var(--ctp-blue) 0%,
			var(--ctp-mauve) 50%,
			var(--ctp-pink) 100%
		);
		height: 0.3em;
		width: 20em;
		margin-bottom: 0.5em;
	}
	.gradient-bar:hover {
		background: linear-gradient(
			to right,
			var(--ctp-blue) 0%,
			var(--ctp-blue) 33.33%,
			var(--ctp-mauve) 33.33%,
			var(--ctp-mauve) 66.66%,
			var(--ctp-pink) 66.66%,
			var(--ctp-pink) 100%
		);
	}
</style>
